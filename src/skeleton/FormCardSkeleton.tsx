import { Skeleton } from '@/components/ui/skeleton';

const FormCardSkeleton = () => {
  return (
    <Skeleton className='border-2 border-primary/20 h-[190px] w-full bg-muted' />
  );
};

export default FormCardSkeleton;
